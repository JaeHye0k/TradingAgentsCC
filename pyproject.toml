[project]
name = "tradingagentscc"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "yfinance>=0.2.63",
    "stockstats>=0.6.5",
    "pandas>=2.3.0",
    "python-dateutil>=2.9.0",
    "markdown>=3.5",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["tools*"]
